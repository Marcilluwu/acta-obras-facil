import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'actasseguridad.istalia.synology.me',
  appName: 'Actas Seguridad Ingeniatura',
  webDir: 'dist'
};

export default config;
